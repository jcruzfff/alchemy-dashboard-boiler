import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Hot reload is enabled by default in development
  // Fast Refresh is enabled by default with Turbopack
};

export default nextConfig;
